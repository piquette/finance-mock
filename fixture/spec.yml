---
services:
  "yfin":
    paths:
      "/v7/finance/quote":
        parameters:
        - description: "Specifies which symbols to provide quotes for."
          name: symbols
          required: true
        resource: quote
